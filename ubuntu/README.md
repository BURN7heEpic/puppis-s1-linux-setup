# Ubuntu/Debian Installer

This folder contains the Ubuntu/Debian-specific installer used by the main Puppis S1 Linux Setup installer.

Do not run this installer directly unless you know what you are doing.

The root installer automatically selects this installer on Ubuntu/Debian systems.

## Manual usage

Install:

```bash
sudo ./install.sh
```
