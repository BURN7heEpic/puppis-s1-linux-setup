# Arch Linux Installer

This folder contains the Arch Linux-specific installer used by the main Puppis S1 Linux Setup installer.

Do not run this installer directly unless you know what you are doing.

The root installer automatically selects this installer on Arch Linux and SteamOS.

## Manual usage

Install:

```bash
sudo ./install.sh
```
